# QQQSP 2.11 源码部署与升级说明

应用版本：**2.11.0**；静态资源：**86**。交付日期：2026 年 9 月 14 日。

## 一、部署概况

本包包含完整运行源码、已构建前端、预压缩资源、配置模板、回归测试及运维脚本。保留单进程和零运行时第三方依赖，生产无需安装开发依赖。最低要求 Node.js 22.16；本轮本地实际验证 22.16.0，Node 24 矩阵已配置但未在当前环境执行。

本版新增基础信息区：默认 16 项、展开 24 项，包括换手率、市净率、市值和股本，所有卡片同步展开。财务资料默认后台缓存 6 小时，不阻塞报价，也不刷新报价时间。量比、委比、每手数量等来源未提供的字段仍显示缺失，不能将其理解为已经接入完整盘口。

继续保留 2.9 引入的“显式持久自选＋在线连接订阅”。已有 2.8 历史状态可以恢复原名单；只读行情、历史和诊断接口不修改持久名单。前端每次实际新增或删除自选时显式保存，定位已有标的不保存也不重连。

## 二、部署前检查与备份

升级保留共享目录，不应以新配置模板覆盖已有凭据、端口或状态。以下示例使用原安装目录 `/opt/qqqsp-v2`；不同安装应替换为实际路径。

```bash
node --version
command -v node
systemctl --version

# 已有安装时执行；首次安装跳过本段
sudo readlink -f /opt/qqqsp-v2/current
sudo systemctl stop qqqsp-v2
sudo tar -czf "/root/qqqsp-shared-$(date +%Y%m%d-%H%M%S).tar.gz"   -C /opt/qqqsp-v2 shared
sudo systemctl start qqqsp-v2
```

备份可能包含供应商或通知凭据，应限制访问。服务账户必须能够执行 Node；仅 root 可进入的私人运行环境不适合作为 systemd 的 Node 路径。

## 三、安装或升级

使用完整源码包解压后运行安装器，无需先安装 2.8。安装器复制生产文件，不把测试样本或截图安装到服务目录。

```bash
unzip QQQSP-2.11.0-v86-full-source.zip
cd qqqsp-v2.11

# Linux 可选的逐文件完整性检查
sha256sum -c SHA256SUMS

sudo env NODE_BIN="$(command -v node)"   bash ops/install.sh /opt/qqqsp-v2 8568
sudo systemctl status qqqsp-v2 --no-pager
```

升级时 `shared/.env` 的 PORT 优先于命令参数；首次安装才采用示例端口 8568。安装器通过独占锁防止同时升级，建立独立发布目录，服务就绪检查失败时尝试恢复上一发布并清理本次失败目录。

```text
/opt/qqqsp-v2/
  current -> releases/本次发布/
  previous -> releases/上一发布/
  releases/
  shared/
    .env
    state/quotes.json
    state/macro.json
    state/history.json
    logs/panel.log
```

浏览器更新就绪后使用提示中的刷新操作，或手动刷新页面，确认资源地址中的版本为 `panel.bundle.js?v=86`。不需要清空浏览器自选、localStorage 或共享状态。更新提示不会自行刷新并打断阅读。

## 四、持久自选与分层历史准备

持久名单决定关闭全部页面后仍持续维护的证券；在线页面名单只在连接存续期间合并，不会缩减持久名单。本项目仍是单用户服务，不提供账户隔离。

已有 2.8 的状态名单会自动恢复。**新安装需要持久维护默认标的时，应在页面实际增删一次自选，或明确提交名单：**

```bash
# 显式保存；最多 12 项。此命令会替换当前持久名单。
curl --fail-with-body -X POST http://127.0.0.1:8568/api/history/watchlist   -H 'Content-Type: application/json'   --data '{"symbols":["QQQ","SPY"]}'

# 只读查看：不会更改名单
curl --fail http://127.0.0.1:8568/api/history/status
```

清空持久名单用同一接口提交 `{"symbols":[]}`。若其他页面仍在线，它们的临时订阅仍可能维护证券；全部连接关闭后释放。状态接口中的 `persistentWatchlist`、`watchlist`、`owners`、`overflow` 分别表示持久名单、实际合并名单、在线所有者数及超额未纳入的证券。

每个名单最多 12 项；服务端合并预算默认 24 项，持久名单优先，其次按在线连接登记顺序纳入，超额可观察。这里的 24 是后台合并预算，不是把单页面自选上限改为 24。

冷启动先请求近端日线、准备四档可用结果；全部近端任务处理后，在无来源队列争用的空闲阶段扩展长历史。日／周／月期望窗口为 79 根、年线为 39 根，但近端阶段较长周期的实际根数可能不足，上市时间和上游覆盖也会限制结果。**没有缓存时仍需要首次合法响应；不能承诺冷启动或公网秒开。**

```dotenv
HISTORY_BACKGROUND_ENABLED=1
HISTORY_STATE_PATH=./state/history.json
HISTORY_REFRESH_MS=60000
HISTORY_TOTAL_DEADLINE_MS=20000
HISTORY_EXECUTION_DEADLINE_MS=25000
HISTORY_EXPAND_AFTER_MS=5000
HISTORY_MAX_SYMBOLS=24

MACRO_BACKGROUND_ENABLED=1
MACRO_STATE_PATH=./state/macro.json
MACRO_CONTEXT_MS=30000
MACRO_NEWS_MS=60000
```

总期限包含排队且优先约束操作，执行期限是独立的上游上限；默认总期限 20 秒不是 20 秒排队后再执行 25 秒。前端默认历史总期限 22 秒，超时保留旧图并允许重试。来源最小间隔、失败冷却及 Retry-After 继续生效，不通过无限并发绕过限速。

后台每分钟检查，不代表来源每分钟提供新蜡烛。`HISTORY_STATE_PATH` 留空停用历史持久化；`HISTORY_BACKGROUND_ENABLED=0` 关闭后台，仍可按需读取。相对状态路径以服务工作目录为基准；安装器将 state 与 logs 指向共享目录。

## 五、资源、日志与版本保留

本版提供可见的容量边界，默认值是部署起点，不是目标机器压测承诺。按实际证券数量、页面数及来源行为观察后调整，避免低内存机器直接采用不合适的上限。

| 配置 | 默认值 | 范围与作用 |
|---|---:|---|
| HTTP_ACTIVE_MAX / HTTP_QUEUE_MAX | 24 / 48 | 常规请求活动及等待数；行情与历史另设独立入口队列 |
| HOST_QUEUE_MAX | 32 | 单来源等待容量 |
| HISTORY_MAX_ACTIVE / HISTORY_MAX_QUEUE | 2 / 16 | 历史实际执行及等待容量 |
| SSE_MAX_CONNECTIONS | 32 | 行情与宏观合计连接数；通常一个页面使用多条流 |
| SSE_MAX_BUFFER_BYTES | 8 MiB | 两类流共享缓冲预算；单连接另限 1 MiB |
| HISTORY_MAX_BYTES | 48 MiB | 原始历史及聚合预留预算 |
| RECOVERY_MAX_BYTES | 8 MiB | 报价恢复数据预算 |
| CACHE_BUDGET_BYTES | 64 MiB | 上述历史、恢复与 SSE 预留之和；不等同于进程 RSS 上限 |
| LOG_BUFFER_BYTES | 256 KiB | 每个日志输出队列的字节上限，超额丢弃可统计 |

安装器默认生成 `MemoryHigh=512M`、`MemoryMax=768M`、`TasksMax=128`、`LimitNOFILE=4096`。需要调整时在执行安装器时显式传入：

```bash
sudo env NODE_BIN="$(command -v node)"   QQQSP_MEMORY_HIGH=512M QQQSP_MEMORY_MAX=768M   QQQSP_KEEP_OLD_RELEASES=3 QQQSP_RELEASE_BUDGET_BYTES=536870912   bash ops/install.sh /opt/qqqsp-v2 8568
```

默认保留 current、previous 和最近 3 个其他版本，受 512 MiB 发布目录预算约束。只清理 releases 内非受保护版本，不遍历共享状态链接；如果 current 和 previous 本身已超过预算，会报告超额而不为满足预算删除回滚锚点。日志队列丢弃数、采样数、来源等待、连接缓冲及事件循环延迟可从诊断读取；`/api/stats` 的访问令牌遵循既有 STATS_TOKEN 配置。

## 六、上线检查与容器探针

“进程存活”“本地服务就绪”和“真实行情可用”是不同结论。`/healthz` 用于存活观察；`/readyz` 在本地磁盘临界等未就绪状态返回失败，`dataReady` 单独表示业务数据状态，避免仅因外部来源暂不可用造成持续重启。

```bash
node ops/smoke.mjs http://127.0.0.1:8568
node ops/history-prewarm-smoke.mjs http://127.0.0.1:8568
node ops/history-prewarm-smoke.mjs http://127.0.0.1:8568 --ready --wait=120
node ops/macro-smoke.mjs http://127.0.0.1:8568 --stream
node ops/macro-smoke.mjs http://127.0.0.1:8568 --diagnose

# 探针使用实际端口，HTTP 200 但 ready=false 也会失败
PORT=8568 node ops/healthcheck.mjs
```

`--ready` 观察当前名单是否有四档可用数组，不强制来源绕过冷却，不保证已经获得完整长历史或最新成交。首次来源不可用会打印失败与状态，不能把旧缓存当作新数据。

Dockerfile 的探针同样执行 `ops/healthcheck.mjs`，从容器环境读取 PORT；改变容器内端口时同步修改映射。当前交付未实际构建、运行 Docker 镜像；目标容器仍需按此口径验收。

```bash
# 示例，仅暴露到宿主回环；持久目录权限需由部署者配置
docker build -t qqqsp:2.11.0 .
docker run -d --name qqqsp-v2 --restart unless-stopped   -p 127.0.0.1:8568:8567   --mount type=volume,source=qqqsp-state,target=/app/state   --mount type=volume,source=qqqsp-logs,target=/app/logs   qqqsp:2.11.0
```

## 七、入口安全与回滚

默认服务监听回环地址；通过公网代理、隧道或容器端口开放访问时，应在入口配置身份认证或访问控制。PUBLIC_ORIGIN 仅用于跨域响应策略，**CORS 不是身份认证**。允许来源在相应 API、SSE、HEAD 和 OPTIONS 上使用一致的响应头；不把本版解释为任意跨域前后端分离的一键配置。

反向代理应关闭 `/api/stream` 和 `/api/macro/stream` 的响应缓冲及缓存，保留长连接。合并到既有 Nginx 站点时可参考：

```nginx
location ~ ^/api/(macro/)?stream$ {
    proxy_pass http://127.0.0.1:8568;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_cache off;
    proxy_read_timeout 75s;
}
```

安装器不修改隧道、域名、防火墙或代理。不应为本版本覆盖整个站点配置。宏观 Webhook 继续使用原有可选 HTTPS 接收端配置，不新增原生离线系统通知或模型调用。

```bash
sudo bash ops/rollback.sh /opt/qqqsp-v2
sudo systemctl status qqqsp-v2 --no-pager
```

回滚切换程序目录，保留 shared 配置与状态，不自动还原数据。首次安装无 previous 时不能使用该回滚路径；数据恢复使用部署前备份。生产 systemd 升级、失败恢复与代理链路尚未在用户机器实装验证。

## 八、基础信息配置与验收

基础资料功能默认开启；无需在原有配置追加全部参数即可运行。升级保留共享配置、供应商凭据和已有持久状态，不以新模板覆盖旧配置。

```dotenv
FUNDAMENTALS_ENABLED=1
FUNDAMENTALS_TTL_MS=21600000
FUNDAMENTALS_RETRY_MS=300000
FUNDAMENTALS_MAX_AGE_MS=604800000
# 可选，仅使用自己的有效凭据；不填仍会尝试默认来源
FINNHUB_TOKEN=
```

打开一只股票激活订阅后，查看“基础信息”和“口径与来源”。通过 `/api/sources` 的 `fundamentals` 项检查开关、缓存与队列；通过 `/api/market?symbols=AAOI` 检查逐字段状态。初次异步读取、公开接口受限或来源缺字段时可显示“—”，不是使用测试数据填充。

## 九、交付与验收结论

本包包含全量源码及已构建资源，可以独立部署，不需要手动合并旧版补丁。版本与布局实现见 `docs/RELEASE-2.11.zh-CN.md`，本轮测试结果见 `docs/TEST-REPORT.zh-CN.md`，原始证据见 `docs/evidence/v2.11-release/`。

真实供应商可用性、账户权限、目标服务器安装和长期负载仍需上线核验，不以受控测试或旧版报告替代。升级应保留共享配置、持久数据与可用回滚版本。
