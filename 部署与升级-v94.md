# QQQSP 2.14.0 部署与升级说明

## 一、交付概览

本版基于用户提供的2.13.0源码，静态版本94，生产零第三方npm运行时依赖。完整包包含服务器、已构建网页、管理页面、客户端、模式、测试和证据。所有真实配置、密钥、日志、采样及缓存都不随包交付。

## 二、首次部署

Node.js不低于22.16。在完整解压目录执行：

```sh
node scripts/init-config.mjs
npm start
```

默认监听127.0.0.1:8567。初始化生成不同的STATS_TOKEN和LLM_API_KEY，写入私有.env，不显示到终端；文件已存在或为符号链接时拒绝覆盖。可先用`node --check server.js`做基本语法核查，生产无需npm install。

Linux systemd可使用：

```sh
sudo bash ops/install.sh /opt/qqqsp-v2 8568
```

首次安装如没有自己的.env，安装器自动调用初始化脚本生成密钥；如已有.env则使用自己的配置。已有shared/.env不会被覆盖。安装器默认新端口8568，不自动停止其他旧版服务或改反向代理路由。只在目标主机已具备systemd和可执行Node时使用；本次交付未在你的主机执行该操作。

管理入口`/api-keys.html`使用STATS_TOKEN，可建最小权限只读密钥。管理和调用必须在HTTPS或本机回环中进行；CORS许可不等于身份认证。页面密钥只保留内存，服务端托管摘要在state/api-keys.json，原子写入且文件0600。

## 三、升级与密钥迁移

保留.env、state、logs；先备份，再切换到新代码目录。普通源码升级应复制持久化目录或保留现有共享目录链接，不能清空后声称历史和密钥已迁移。使用现有安装器时release目录与shared状态分离。

新增可选配置：LLM_KEY_STORE_PATH（默认./state/api-keys.json）、API_V1_DEPRECATION_AT、API_V1_SUNSET_AT。旧配置无需强制添加所有新键，缺省值由config.js填入。旧LLM_API_KEY仍然接受；创建托管密钥后，调用端使用新密钥所需scope。清空环境密钥只关闭该环境密钥，不关闭其他托管密钥。

轮换不会改你的.env明文；会在密钥状态里记录旧密钥宽限及新密钥摘要。新钥明文仅显示一次，请保存至私有客户端环境。旧钥24小时后401 KEY_ROTATED。不要删除state/api-keys.json来“恢复”：删除轮换／撤销状态可能重新启用旧.env密钥。此文件与.env必须一并备份，并限制文件读取权限。

**回退特别说明：** 2.13.0及更早程序不理解多密钥状态。回退旧程序前先在反向代理关闭只读API入口，重新生成／替换旧程序将读取的LLM_API_KEY及客户端凭据，再恢复入口；不能认为新的撤销记录会被旧程序执行。回退同一2.14实现时仍保留完整state和.env。不要把曾泄露、已撤销的密钥重新填回配置。

## 四、数据源与真实能力

显式raw/split/split_dividend、授权长期分钟历史和全市场涨跌活跃榜单使用自己的APCA_API_KEY_ID/APCA_API_SECRET_KEY。配置这两个键即可启用相应HTTP资料路线，不必开启ALPACA_ENABLED报价流；ALPACA_FEED默认iex，并不代表全美SIP覆盖。是否有相应权限以供应商实际响应为准。

未配置时原网页行情及旧功能保留；请求高级功能得到NOT_CONFIGURED或明确部分结果。公开分钟来源仅在近7天请求预算内尝试，不保证该范围一定有数据。公司行动源没有可靠币种、支付日期或完整覆盖时，事件字段保持null，事件不能强制补成已支付股息。

新高级缓存最多8条、每条序列化数据2MiB，独立于旧历史／恢复／SSE缓存预算；JavaScript对象、队列及传输缓冲有额外开销，预算不是RSS硬上限。现有systemd MemoryHigh/MemoryMax仍按进程约束。密钥状态最多256条，不自动抹掉撤销记录；达到容量时应受控迁移，不直接删除状态。

## 五、客户端与验证

客户端私有环境文件包含LLM_API_KEY和LLM_API_BASE_URL，使用服务器的只读密钥而不是管理员密钥。示例：

```sh
node --env-file=/private/client.env scripts/market-detail-client.mjs NVDA --output detail.json
node --env-file=/private/client.env scripts/market-api-client.mjs NVDA,SPY --include quote,fundamentals --output batch.json
node --env-file=/private/client.env scripts/market-api-client.mjs NVDA --include daily --adjustment split --daily-bars 100 --output adjusted.json
```

每个输出文件必须不存在；JSON全部不可用时仍保存结构化诊断，退出码2。CSV需要一个时序分区，将CSV与自动生成的.metadata.json同时交给智能体；不要只保存可见截断文本。

开发验证：

```sh
npm run build
npm run check
npm test
npm run test:v94
npm run test:v94-browser
npm run test:v94-regression-browser
python3 scripts/package.py --verify /path/to/qqqsp-v2.14.0-source.zip
```

开发时可npm ci；无法联网安装时，模式测试可使用已安装的Python jsonschema。浏览器测试需要Python Playwright与Chromium，可设置CHROMIUM_BIN=/usr/bin/chromium。生产服务不需要这些测试依赖。

## 六、验收与结论

上线验收至少检查healthz、静态版本94、无钥401、受限权限403、批量配额、持久化重启、轮换宽限、真实授权历史与目标证券。出现SOURCE_PERMISSION_DENIED时应核对自己的权限，而不是更改来源标签或填入测试数据。成品验证日志在docs/evidence/v94；生产主机切换、真实供应商权限和容器镜像构建应单独记录，不与离线测试混为一谈。
