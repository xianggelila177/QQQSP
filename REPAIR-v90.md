# v90 日经225报价与推送修复

## 原因与处理

`^N225` 已在服务器保存的自选名单中，但此前没有批量报价适配器，也没有备用历史接口。它依赖的 Yahoo 返回 HTTP 429；首次报价失败又被缓存服务隐藏成永久 `PENDING`，因此网页虽然连接了推送，也收不到日经价格。

已按“先复现、补充指数来源、修正失败与恢复状态、测试后上线”的计划完成：

- 为日经现金指数接入已核验的 Naver `.N225` 路由，严格核验证券、交易所、时区及价格时间；保留 Yahoo 备用路径。
- 接入原有服务器后台订阅和 SSE；多个网页共用报价请求。遵守约 70 秒的来源检查间隔、缓存截止时间和 Retry-After。
- 来源失败时显示明确错误及重试元数据，恢复后清除故障；不再无限显示“等待首次报价”。旧价格仍不能覆盖较新价格。
- 提供真实分时与日线，日线聚合为周/月/年线；默认显示来源历史。指数以“点”显示，来源的汇总成交量不冒充指数份额成交量。
- 来源明确声明 15 分钟延迟，页面据实显示；发布时间与成功检查时间分开，休市后不伪造新成交。

## 验证

- 新增 11 项测试覆盖 429、首次失败恢复、成功后断源切备用、东京时间、错误证券、异常价格、后台采集、多个 SSE 客户端、检查间隔及缓存截止时间。
- 原有回归 91 个文件通过；核心测试 362 项通过；Chromium/WebKit 新增端到端测试均通过。
- 新适配器覆盖率：行 100%，分支 82.12%，函数 97.14%。代码审查无阻断问题。
- 服务器候选版本实测：报价 64,136.25 点、333 个分时点；日/周/月/年历史均成功，较大范围查询可返回 2010 年以来的记录，尊重来源可提供的实际范围。
- 发布后公开接口、两种真实浏览器均验证成功。SSE 两帧成功检查时间分别为 1789653597141 和 1789653667291，相隔 70.150 秒；收盘价和来源发布时间保持不变，图表通过 `same` 标记复用。
- 保存的 11 只自选前后完全一致；其余后台功能和现有服务器配置保留。v90 静态资源与 Service Worker 版本一致。

## 发布与回退

现行版本：`/opt/qqqsp-v2/releases/20260917-nikkei-v90`，软件版本 `2.11.1`。

上一版：`/opt/qqqsp-v2/releases/20260915-macro-v89`，`/opt/qqqsp-v2/previous` 指向该版本。

上线前状态备份：`/opt/qqqsp-v2/backups/pre-nikkei-v90-state.tar.gz`。发布采用锁、原版本文件校验、原子切换和失败自动回退；只重启 `qqqsp-v2.service`。

日经当前已休市，价格不变化不代表推送停止。开市后按来源更新；该来源有 15 分钟延迟，不是交易所实时逐笔流。

身份与价格交叉核验：[日经指数官方页面](https://indexes.nikkei.co.jp/en/nkave)、[Naver 日经225](https://m.stock.naver.com/worldstock/index/.N225/total)。
