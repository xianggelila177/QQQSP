# QQQSP v2：直接启动默认配置即可；可选流式来源及结构化经济日历需要自有凭据。
# 时间间隔单位为毫秒；开关为 0 或 1。请勿将 .env 提交或公开。
# HOST 默认仅回环；systemd 安装器默认使用 8568，避免占用旧版端口。
# ALPACA_ENABLED=1 后配置 APCA_API_KEY_ID / APCA_API_SECRET_KEY；iex 不是全美市场。
# FINNHUB_TOKEN 可留空；具体实时覆盖与权限取决于账户。
# TE_API_KEY 可留空；仅使用自己的 Trading Economics 日历权限，不提供公共密钥。
# HISTORY_BACKGROUND_ENABLED=1 日周月年共享日线后台预备；HISTORY_STATE_PATH 保存显式提交的后台自选及历史；只读 GET 不改名单。
# 历史先预备近端，空闲再扩展长历史；在线 SSE 名单按连接合并，默认总上限24。
# CACHE_BUDGET_BYTES 约束历史/恢复/SSE 的预算和，不等于进程 RSS 上限。
# PUBLIC_ORIGIN 是 CORS 许可，不是认证；公网入口请配置反向代理身份认证。
# HISTORY_REFRESH_MS 默认60000毫秒；空历史仍需等待首次合法来源响应。
# MACRO_BACKGROUND_ENABLED=1 默认开启服务器独立采集，关闭页面不会停止。
# MACRO_STATE_PATH 为持久化文件，空值只停用持久化，不停止采集。
# MACRO_WEBHOOK_URL 可选 HTTPS JSON 接收端；未配置时只向在线页面推送。
# MACRO_WEBHOOK_TOKEN 仅在服务端发送 Bearer 请求头。
# FX_MODE=reference 使用日度参考汇率并显示近似标记。
HOST=127.0.0.1
PORT=8567
SYMBOLS=QQQ,SPY
PUBLIC_ORIGIN=
STATS_TOKEN=
REALTIME_SNAPSHOTS=1
PUBLIC_SOURCE_REDUNDANCY=1
RECOVERY_PATH=./state/quotes.json
FX_MODE=reference
POLL_MS=1000
POLL_PRIMARY=sina
CACHE_MS=2000
QUOTE_MAX_AGE=10000
UPSTREAM_TIMEOUT=8000
UPSTREAM_MAX_BODY_BYTES=2097152
Y429_BASE=30000
Y429_CAP=900000
Y_TASK_DEADLINE=20000
HOST_QUEUE_MAX=32
HOST_DEADLINE_MS=20000
HTTP_ACTIVE_MAX=24
HTTP_QUEUE_MAX=48
HTTP_CLIENT_QUOTA=120
HTTP_SNAPSHOT_QUOTA=600
TRUST_PROXY_LOOPBACK=0
HTTP_REQUEST_DEADLINE_MS=20000
HTTP_SEARCH_DEADLINE_MS=12000
HTTP_QUOTE_ACTIVE_MAX=8
HTTP_QUOTE_QUEUE_MAX=16
HTTP_HISTORY_ACTIVE_MAX=4
HTTP_HISTORY_QUEUE_MAX=16
HISTORY_TOTAL_DEADLINE_MS=20000
HISTORY_EXECUTION_DEADLINE_MS=25000
HISTORY_MAX_ACTIVE=2
HISTORY_MAX_QUEUE=16
HISTORY_MAX_SYMBOLS=24
HISTORY_EXPAND_AFTER_MS=5000
HISTORY_MAX_BYTES=50331648
RECOVERY_MAX_BYTES=8388608
SSE_MAX_CONNECTIONS=32
SSE_MAX_BUFFER_BYTES=8388608
CACHE_BUDGET_BYTES=67108864
ENRICH_OPEN_MS=60000
ENRICH_CLOSED_MS=900000
ENRICH_BREAK_MS=300000
DISK_PATH=
LOG_BUFFER_BYTES=262144
LOG_SAMPLE_LIMIT=20
SLOW_TTL=300000
NEWS_TTL=540000
NEWS_FAILURE_COOLDOWN=60000
NEWS_MAX_ENTRIES=48
NEWS_MAX_ACTIVE=12
AUTH_TTL=3300000
AUTH_FAILURE_COOLDOWN=60000
FX_FAILURE_COOLDOWN=60000
ALPACA_ENABLED=0
ALPACA_FEED=iex
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
ALPACA_MAX_SYMBOLS=12
ALPACA_SNAPSHOT_MS=60000
ALPACA_REST_MIN_GAP_MS=2000
ALPACA_ACTIVE_TTL_MS=300000
HISTORY_BACKGROUND_ENABLED=1
HISTORY_STATE_PATH=./state/history.json
HISTORY_REFRESH_MS=60000
MACRO_BACKGROUND_ENABLED=1
MACRO_STATE_PATH=./state/macro.json
MACRO_CONTEXT_MS=30000
MACRO_NEWS_MS=60000
MACRO_WEBHOOK_URL=
MACRO_WEBHOOK_TOKEN=
TE_API_KEY=
FINNHUB_TOKEN=
LOG_LEVEL=info
LOG_FILE=./logs/panel.log
LOG_MAX_BYTES=10485760
LOG_MAX_FILES=3
