# QQQSP 2.13.0 交付与验证报告

## 一、交付概况
以用户上传的 `QQQSP-main.zip`（应用2.12.0、静态92）为基线，交付应用2.13.0、静态93。本次没有推送远端仓库、连接或改动用户服务器，也没有复制用户私有配置。

交付包含完整源码、构建产物、旧版兼容接口、新版证券详情接口、客户端、网页详情、测试、安装与回滚文档。生产保持一个Node进程、零npm第三方运行时依赖。

原始压缩包的校验摘要、扫描范围与测试输出在 `docs/evidence/v93`。目录枚举和静态扫描覆盖提交材料；关键链路进行了人工复核和运行验证，不能将其等同于所有行为的形式化证明。

## 二、已实现的修改

### 数据与智能体接口
新增 `/api/v2/market-detail`：快照档位只取得报价、独立盘口及财务字段；分析档位增加来源分时、日线、采样和已有资讯/宏观缓存。默认具名对象序列，也可使用紧凑列式序列。旧v1接口保留，两个版本共用鉴权限额和工作队列。

财务固定24个字段，包括原有基础指标、五十二周区间及估值输入。缺失、不适用、冲突、旧值明确表达；补充中文标签、单位、公式、计算输入、财务期间、分母、股本来源和重试记录。覆盖率不再只对实际出现的字段计数。

报价、分时、财务分别判断完成状态；财务超时不再污染已经成功的报价状态。历史续页参数传给既有历史服务，保留来源序列身份，避免跨页混用口径。

### 买卖盘口
盘口与成交拥有独立身份、时间和数量单位。新增读取Alpaca的quotes流及快照latestQuote，并接通腾讯沪深报价已有盘口字段。盘口纳秒顺序与成交时间分别处理；迟到盘口不会覆盖较新盘口，盘口更新不会人为“刷新”成交时间。

保留Alpaca的round_lots数量，不统一乘100；沪深腾讯来源保留lots。单一交易所报价不宣称是全市场最优报价。不使用一档数量替代完整委比；不以最后价格乘成交量冒充成交额。

### 网页与同步
每张卡片新增“完整数据”窗口。窗口分组成交、当日统计、盘口、市值股本、估值、分红及计算依据，可展开查看来源和公式，并导出完整JSON。窗口显示原生币种，避免与卡片显示币种转换混淆；支持手机宽度、键盘关闭与焦点恢复。

自选写入增加管理员鉴权，未配置密钥的本机例外拒绝代理头和错误Host。页面提供仅当前会话内存的管理密钥输入，不写入localStorage/sessionStorage，不向页面分发只读智能体密钥。同步失败保留最新待保存版本并限次退避，鉴权失败不无限重试。

### 结构与性能
抽取纯图表修订模块，消除行情业务模块对HTTP工具的反向依赖；统一金融数值、原始正常交易统计及价格单位换算。输出构建中的公共值和财务投影拆成独立模块。五十二周高低点须匹配来源、币种、单位、口径和观测时间。

日期格式化器使用64项有界缓存；同一请求的当前交易日期只计算一次。财务实时记录按证券索引，避免逐字段重复扫描全量记录。聚合等待采用订阅事件加一秒兜底，而非100毫秒反复读取。采样增量直接复用元数据，不重复调用完整snapshot。

公共数据源诊断改为汇总输出，证券级详细失败信息留在管理员诊断接口。面板整体仍为单用户公开只读设计；这些改动不等于全站隐私隔离，私有部署应在反向代理增加全站认证。

## 三、实际验证结果

|验证|结果与说明|
|---|---|
|核心测试|448项通过，0失败、0跳过；新增v93专项22项包含于其中|
|保留回归|91个测试文件通过；43个既有退役文件不计为通过|
|构建检查|444个脚本语法、版本、静态构建、预压缩、配置一致性检查通过|
|客户端回归|原客户端13项通过，新增版本、身份、鉴权和协议边界测试通过|
|JSON Schema|同一2020-12标准的正例、缺字段和多列反例验证通过|
|浏览器详情|1440与375宽度；24字段、盘口单位、公式、导出、缺失值、键盘焦点和管理员重试通过；无页面脚本错误|
|原基础信息浏览器|存储可用/不可用、16/24项切换、币种与估值、报价年龄、320～1730宽度回归通过|
|历史与交互浏览器|原历史预热/周期切换回归，以及修复交互集通过|
|成品包冷启动|解压到无node_modules的新目录；真实生产入口启动、静态资源字节一致、接口鉴权、客户端保存不可用诊断、正常退出通过|
|静态导入扫描|145个生产源模块、11150行；未检出静态ES导入循环，不包含动态依赖与window依赖的证明|
|凭据扫描|0项发现；不包含私有.env、运行状态或依赖目录|

基线检查通过。基线核心测试的唯一失败来自环境无法安装Ajv开发依赖；本次增加测试专用标准验证器，在Ajv已安装时使用Ajv，否则调用Python jsonschema。未删除原有结构断言，也未把验证失败改为跳过。

本次无法联网执行npm安装，因此没有宣称运行了完整的npm Playwright跨浏览器矩阵。Python Playwright与Chromium可用；环境策略禁止Chromium直接访问本机地址，涉及真实应用的浏览器测试通过测试桥接转发实际Node HTTP/SSE响应。桥接只用于测试，不存在于生产程序。原生Node HTTP/SSE另有集成测试；原生浏览器重连、代理缓冲和系统后台挂起仍需目标环境验收。

修改过程中发现并修复：生成Schema的共享引用重复required字段、测试版本演练未复制新生成器，以及旧滚轮测试假定图表固定在顶部的问题。保留了首次失败及最终通过证据，没有将这些失败静默移出测试。

## 四、性能对照
离线确定性样本：390条分时、500根日线、1170条服务器采样；Node22.16.0；3次预热后20次测量。测量包含纯接口组装和JSON序列化，不含网络、排队或供应商响应。

|分析窗口组装|修改前|修改后|
|---|---:|---:|
|中位耗时|55.283毫秒|11.815毫秒|
|95%分位耗时|62.274毫秒|14.400毫秒|
|未压缩响应|159006字节|170552字节|

95%分位耗时在本组样本下降约76.9%。响应增加约7.3%，是固定缺失字段和计算证据的成本，不是压缩丢字段。快照样本的未压缩响应从2746增加到14255字节，组装中位耗时为0.137毫秒；这不是实际网络请求总耗时。

该结果是单机基准，不代表所有证券、并发或供应商延迟。每秒报价年龄更新不意味着源市场每秒有新成交。

## 五、尚未验证及保留的边界
本次验证不包含用户服务器上的systemd切换、Docker镜像构建、真实商业行情授权、实时供应商联调和长时间生产负载。因此，不能承诺截图中的每一个数值在任意市场都可返回，尤其是完整订单簿、委比、同口径量比及缺失财务日期。

SKHY及其他商家特有代码没有未经核验的跨市场别名映射。应使用已支持的规范证券代码并核对返回身份、交易所、币种。腾讯字段路线基于项目已有解析布局与离线验证，仍需观察供应商日后是否变更。

本次没有整体重写 `http.js`、`snapshot-service.js`、主应用装配或既有图表控制器；高风险的大规模拆分保留到单独的行为等价重构。原项目各供应商的服务条款、许可、可访问性、接口变化和交易所日历更新仍需持续维护。

## 六、使用总结
完整部署步骤见 `部署与升级-v93.md`，智能体调用见 `public/market-detail.md`，修改前清单见 `docs/AUDIT-PLAN-v93.md`。首先保留原配置和状态，再部署程序；检查实际来源状态和每段缺失原因，而不是把HTTP成功等同于行情完整。
