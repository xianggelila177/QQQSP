# v108 审查修复记录

日期：2026-09-26。对应本次审查的 20 项问题，修复基于当前完整工作区，保留已有未提交修改。资源版本由 107 更新为 108，应用包版本仍为 2.16.3。本次完成本地修复、构建和有界验证，未部署。

## 问题与处理

| 原编号 | 问题 | 已实施处理 |
| --- | --- | --- |
| 1 / F1 | 切换证券后残留旧盘口 | 打开详情同步清理身份、标题、价格、盘口、逐笔；响应检查证券和范围；失败时明确不可用。 |
| 2 / R1 | 旧打包入口携带私有配置 | 两入口共用文件白名单；排除 private、状态、输出、凭据类文件和符号链接；验证 ZIP 全部成员与哈希。 |
| 3 / R2 | Shell CRLF 导致上线失败 | Shell 统一 LF，增加 .gitattributes；构建检查和打包均拒绝 CR；平台跳过的语法检查明确报告。 |
| 4 / B1 | 恢复历史后丢失最新交易日待确认状态 | 校验并恢复 latestSessionPending，保留 stale 语义。 |
| 5 / B2 | 旧分时错误显示 ready | regularChart 保留实际来源的 stale、error、sourceCheckedAt 和 missingReason；主卡与详情解释旧图状态。 |
| 6 / B3 | 手动刷新迟到响应写回已移除证券 | 常规与手动刷新共用提交函数，检查服务代际、成员、容量、证券身份和报价先后。 |
| 7 / B4 | 期货非正价格跨层被拒绝 | 共用价格值域函数，仅明确 FUTURE 接受有限零价/负价；供应商、历史、恢复、宏观、API schema 与图表同步；零基准百分比为空。 |
| 8 / B5 | 已启用日历等待中仍 complete | waiting/loading 计入 partial，明确给出 pending 原因；禁用的可选日历保持原语义。 |
| 9 / F2 | 期货前结算误标昨收 | 主卡、详情、图表共用报价基准说明，区分前结算、来源前值和前收盘。 |
| 10 / F3 | 详情绕过可见性和冷却策略 | 接入统一网络组件，遵守省流、隐藏页、取消、截止时间和 Retry-After；回到前台补检查。 |
| 11 / F4 | SW 跨版本回退混装 | 安装时验证完整应用壳，静态资源严格按版本匹配；新版壳未准备好时保留完整已安装版本。 |
| 12 / F5 | VWAP 每次逐根创建格式器和重算 | 按时区复用格式器，按序列身份/版本缓存 VWAP；尾点或版本修改后重算。 |
| 13 / 性能1 | regularChart 绕过条件传输 | 独立 bars revision、可选四字段 cv；客户端复用同一数组；SSE 显式协商 regularDelta，旧客户端保持完整数组。 |
| 14 / 性能3 | 自动长历史冲击缓存预算 | 后台只预热近期；切换周/月/年按需加载；near 推送不覆盖完整历史，主卡和详情独立刷新当前周期。 |
| 15 / 性能4 | 历史 SSE 重复传整批 bars | historyDelta 显式协商；仅变化周期传数组，其他只传元数据；首连/重连完整同步，身份或基线不匹配不误清数据。 |
| 16 / 性能6 | active 集合重复扫描、容量不统一 | 维护增量成员和引用计数；共用 200 标的上限；订阅整组准入，超额明确 503；后台采样保留原订阅并可恢复。 |
| 17 / R3 | 回滚缺少锁、就绪确认和恢复 | 安装与回滚共用锁、发布目录校验和原子链接切换；兼容旧部署 VERSION 的 CRLF；检查进程及正确版本的 readyz；失败恢复操作前 current/previous。 |
| 18 / B6 | 撤销密钥永久占槽 | 容量不足时压缩无效托管记录；保留环境密钥撤销墓碑和有效轮换家族，避免已撤销密钥复活。 |
| 19 / F6 | 减少动画偏好无效 | 禁止新增价格 Web Animations，并在偏好变化时取消已有动画。 |
| 20 / R4 | 文档资源版本漂移 | README 从 VERSION 同步，check 检测不一致；旧打包入口仅作兼容转发。 |

## 架构调整

本次按已经出现行为漂移的边界合并实现：快照提交、品种价格值域、报价基准、网络策略、发布白名单和发布切换协议。详情周期与最少显示条数读取公共周期配置，避免独立硬编码。

现有浏览器模块仍通过 window.PANEL_* 装配；构建新增缺失依赖、重复导出和已知初始化依赖顺序检查。没有进行单纯移动文件或整体框架迁移。路由进一步拆分、详情视图进一步分层属于后续维护方向，不把这些结构性建议误报为已经完成的重写。

## 验证与量化

- 新增四组 audit 回归合跑 34/34 通过；随后补充的 HTTP 路由协商回归 1/1 通过，共 35 项，覆盖全部问题的核心触发条件及关键兼容路径。
- npm run build 与 npm run check 通过：520 个 JS/MJS 语法、版本、schema、bundle、预压缩、配置一致性检查。
- 18 个 Shell 文件无 CR，并用本机 Git Bash 单独完成 bash -n 检查。
- 模拟回滚验证成功切换、目标不就绪后恢复、抢锁失败不操作服务；系统命令均为隔离夹具，未操作真实 systemd。
- 受影响的后端 55 项、前端 16 项、引擎与容量等既有定向回归通过；没有运行全套测试。
- 控制器 6 组集成检查通过：包括负价基准、零分母、near 预热后的按需加载、完整长周期继续刷新与冷却；共享浏览器测试入口复用真实构建模块清单，修正新增依赖造成的夹具缺失。
- 合成 390 根分时数据的版本命中 JSON 从 24,420 B 降至 271 B；1950 根图表只创建一个时区格式器，未变序列的后续计算不再逐根格式化交易日。

字节为未压缩合成输入，性能回归为本机 Node 离线结果，不代表移动设备绘制耗时或公网吞吐。1000/3200 标的算法对比只用于提高容量的隔离实例，生产默认容量仍为 200。

## 验证边界

真实浏览器视觉、触摸、窄屏旋转和线上验收尚未执行。服务就绪不等于上游行情完整；本次未访问生产行情接口，也未改变运行中的配置、状态或密钥。

额外运行的旧测试中，Windows 文件权限用例仍按 POSIX 0600 断言失败；一个采样测试的 setImmediate 等待与既有 100ms 事件合并不一致，在原始 HEAD 对照中也失败。保留这些限制，没有以“全套通过”描述本次结果。

主要复验入口：

```text
npm run build
npm run check
node --test tests/v2/audit-backend-repairs.test.mjs tests/v2/audit-frontend-repairs.test.mjs tests/v2/audit-performance-repairs.test.mjs tests/v2/audit-release-repairs.test.mjs
bash tests/audit-rollback-check.sh
python scripts/package.py <output.zip>
python scripts/package.py --verify <output.zip>
```

原始审查目录中的复现脚本断言修复前行为，保留用于证据追溯；修复后请使用仓库中的 audit 回归。
