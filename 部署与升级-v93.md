# QQQSP 2.13.0 部署与升级

## 一、概况
本包包含服务器、网页构建产物、智能体接口、客户端、测试和文档。生产仍是一个 Node.js 进程，无 npm 第三方运行时依赖。已在本次环境验证 Node.js 22.16.0；需要不低于22.16的版本。

本包不包含用户密钥、运行时自选或采样数据。不要用包内新目录覆盖已有的 `.env`、`state`、`logs`。

## 二、首次本地运行
解压并进入 `qqqsp-v2.13.0` 目录：

```sh
node --version
node scripts/init-config.mjs
npm start
```

默认地址 `http://127.0.0.1:8567`。初始化脚本只创建新 `.env`，生成两种不同的随机密钥，文件权限0600，且不在终端输出密钥。配置已存在时脚本会拒绝覆盖，此时直接保留并使用原配置。

初次打开页面添加自选后，点击顶栏“后台同步”，输入 `.env` 中的 `STATS_TOKEN`。页面仅在内存保留该密钥，刷新或离开页面后需要重新输入。本地自选即使同步失败也不会丢失；服务器写入失败时保留队列，限次退避重试，鉴权失败等待手动提供密钥。

不要在浏览器中输入 `LLM_API_KEY`。该密钥专用于本地智能体只读查询。

## 三、Linux 服务安装与升级
沿用现有单服务安装器。首次部署可以先初始化配置，再运行：

```sh
sudo bash ops/install.sh /opt/qqqsp-v2 8568
```

安装器使用独立发布目录，状态和日志位于 shared 子目录；已有 `shared/.env` 原样保留。参数8568只在首次安装时写入配置，升级不会替换已有端口。服务名是 `qqqsp-v2.service`，不停止其他名称的旧服务。

安装器会创建服务用户和服务配置并重启该服务，因此应仅在明确计划升级目标主机时执行。`NODE_BIN` 指向服务用户可以执行的 Node 路径；仅 root 可访问的版本管理器目录不适合作为服务运行路径。

```sh
systemctl status qqqsp-v2.service
journalctl -u qqqsp-v2.service -n 100 --no-pager
```

旧配置没有两种密钥时，应在原私有配置中分别生成并设置。两种密钥必须不同，LLM_API_KEY必须是32～256位URL安全字符。不要把旧配置直接替换成 `.env.example`，也不要将密钥写进公开部署说明、命令行参数或源码。

回滚沿用原安装器保存的上一发布目录：

```sh
sudo bash ops/rollback.sh /opt/qqqsp-v2
```

回滚只切换程序版本；共享配置、自选和历史采样保留。升级前仍应另行备份共享目录。原先不同目录或服务名的部署需按其实际运行配置升级，不能照抄覆盖路径。

## 四、可选容器运行
新增 Dockerfile 与 Compose 配置采用非root进程、只读根文件系统、独立状态/日志卷和回环绑定。Compose 项目名固定为 qqqsp，避免因解压目录版本号变化而创建新的空状态卷：

```sh
node scripts/init-config.mjs
docker compose up -d --build
```

已有 `.env` 时跳过初始化。Compose 将状态写入命名卷，不能自动读取其他部署目录已有的 state。迁移已有数据应停写、备份并显式复制到对应卷。

本次环境没有执行容器镜像构建或目标主机 systemd 安装；上述两种部署方式需在目标环境完成启动检查。生产包中的直接 Node 启动已单独验证。

## 五、验收与访问边界

`/healthz` 验证进程响应；`/readyz` 验证服务基础就绪，**不是所有供应商行情已可用的保证**。需要进一步查看返回中的 `dataReady`、页面来源状态，以及智能体响应中每个数据段的时间与缺失原因。

默认只监听回环地址。需要公网访问时通过现有 HTTPS 反向代理转发；不要直接开放未加访问控制的管理入口。自选写接口需要管理员密钥，未配置密钥时仅允许不带代理头、Host正确的真实本机直连请求。

反向代理转发 SSE 应关闭缓冲并给长连接足够读取超时。浏览器一秒刷新报价年龄不代表供应商每秒产生新成交，也不改变已有授权行情的延迟。

完整智能体用法见 `public/market-detail.md`。测试与边界见 `交付与验证-v93.md`。缺失的供应商授权、盘口、财务期间或成交额不会由本程序伪造补齐。
